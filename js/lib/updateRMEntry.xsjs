$.response.contentType = "application/json";

$.response.setBody("Update Successful");

$.response.status = $.net.http.OK;

//$.response.setBody("Update Failed: Duplicate NS2 RM ID");

//$.response.status = $.net.http.BAD_REQUEST;